DOUBTS?
-- Cli argument is best choice because easy to automate(scriptable) and just one command. --

workflow :-
OrganizerCLI
   ↓
FileOrganizerService
   ↓
FileScanner → Classifier → FileMover

PHASE 1 : Path Input  → Validation

Argument count check
Path validation
Enum-based mode validation
add printUsage() - make private
validate arguments
add PathValidator 
enum OrganizerMode - extension, size

Phase 2 : Scanner observes {
🧨 Edge Case 1: Permission denied inside subfolders
               Files.walk() throws AccessDeniedException - stop entier scan(bad❌) or skip✅
🧨 Edge Case 2: Symlinks
🧨 Edge Case 3: Huge directories

scan(Path root)
Uses Files.walk
Filters regular files
Returns List<Path>
}


Phase 3 : Organizer acts


Layer	            Handles
CLI       	       Bad arguments
PathValidator	    Invalid root path
Scanner	          Per-file access issues
Mover           	 Collisions & permissions

EDGE CASE:-
⚠️ Edge Case 1: Relative paths - taken care by java.nio.file.Path
⚠️ Edge Case 2: Symbolic links
⚠️ Edge Case 3: Permission changes mid-run
⚠️ Edge Case 4: Windows vs Linux paths - taken care by java.nio.file.Path
Empty directories - Just report - FileScanner / OrganizerService